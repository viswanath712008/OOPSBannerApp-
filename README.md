# Java OOPS Program

This is a simple Java program that displays "OOPS" to the console.

## How to Run

1. Ensure you have Java installed (JDK 8 or higher).
2. Compile the program: `javac Oops.java`
3. Run the program: `java Oops`

## Output
```
OOPS
```

## GitHub Setup

To upload this to GitHub:

1. Install Git if not already installed (download from https://git-scm.com/).
2. Initialize Git repository: `git init`
3. Add files: `git add .`
4. Commit: `git commit -m "Initial commit"`
5. Create a new repository on GitHub.
6. Add remote: `git remote add origin <your-repo-url>`
7. Push: `git push -u origin main`